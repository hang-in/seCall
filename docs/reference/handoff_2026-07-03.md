---
type: reference
status: draft
updated_at: 2026-07-03
---

# Handoff 2026-07-03 (Windows session3)

## 1. 한 줄 요약

**v0.6.5 릴리스** + 검색 스택 대개편(외래어 #118 · qwen 임베딩 #120 · 위키 시맨틱 픽스 #121 · 그래프 Adamic-Adar #124) + **main CI red 회귀 복구(#125)**. main green, open PR 0. 다음 = AA 라이브 리빌드 / graph insights(surprising+gaps) / 위키 루프 완주.

## 2. 이번 세션 머지 (전부 main)

- **#118** 검색 외래어 음역 alias + OR/prefix 질의확장 (tunaRound 역이식) — FTS5 특수문자 이스케이프 버그(Gemini) 수정 후 머지.
- **#120** 기본 임베딩 `bge-m3` → `qwen3-embedding:0.6b` + README(4종) + 죽은 링크 `tobi/llm-wiki` 삭제 (Closes #114, 제보 @jeonghyeon-net 감사 댓글).
- **#121** 위키 시맨틱 검색 쿼리 임베딩을 config 경로로 통일 (버그: `collect_semantic_matches` 가 env/OllamaEmbedder 기본값 bge-m3 로 임베딩 → qwen 페이지와 교차모델 불일치. `self.search.embed_query` 로 교체).
- **#122** v0.6.5 릴리스 (버전범프 + CHANGELOG + None→에러 폴백[Gemini #121 리뷰]) → 태그 `v0.6.5` push → 바이너리 macOS(aarch64/x86_64)+Windows 게시. (Linux 는 소스빌드.)
- **#123** README(4종) 참고 프로젝트에 `nashsu/llm_wiki`(13.5k⭐) 추가.
- **#124** 그래프 관련세션 랭킹을 **Adamic-Adar** 로 (`get_related_sessions`: hop+알파벳 → 공유 엔티티 Σ1/ln(deg), agent 허브 제외, 배치 3쿼리로 N+1 회피). `RelatedSession.score` 추가.
- **#125** `wiki_search_modes` 통합 테스트 회귀 수정 (#121 이 깨뜨린 것 — 테스트가 env-stub 방식 가정 → `VectorIndexer(stub)` 주입으로 갱신). **main green 복구.**

## 3. 현재 런타임 상태 (중요)

- **설치 바이너리 `D:\.cargo\bin\secall.exe` = main-HEAD @ 2026-07-03 06:39** — #118/#120/#121 반영. **#124(AA)·#122(v0.6.5 버전) 미반영** (그 후 커밋). → **AA 를 recall 에 켜려면 재리빌드 필요** (`cargo install --path crates/secall --force`, MCP 종료 후).
- config(`%APPDATA%\secall\config.toml`): embedding=`qwen3-embedding:0.6b`, ollama_url=`http://127.0.0.1:11435`.
- **`wiki_vectors` = qwen 42페이지** (재vectorize 완료). 위키 시맨틱 검색 정상 (실측: "로컬 LLM 위임" → tunaLlama 0.775).
- **함정**: `secall wiki vectorize` 의 `--ollama-url` 기본값은 `localhost:11434` 지만 config 는 `11435` → 반드시 명시. (모델은 신 바이너리 기준 qwen 기본이나 URL 은 config 미반영.)
- secall MCP 서버는 install 위해 세션 중 종료됨 — 사용자가 `/mcp` 로 재시작(설치 바이너리=06:39 스냅샷을 물음).
- 참고: 프롬프트 상단 `⚠ Semantic layer: vec/bge-daemon down` = **tunaFlow rawq 레이어**지 secall 아님.

## 4. 위키 심화 루프 (Opus, Task Scheduler)

- `.wiki-loop/` (gitignored): `deep_resume_capped.sh`(Opus, `MAX_PER_RUN=5`, 배치 종료 시 `wiki vectorize --ollama-url :11435` 자동) + `run.cmd` + `deep_done.txt` + `deep_loop.log`. Task Scheduler `seCall-WikiDeepLoop` 5h 간격.
- 17개 PAIRS 중 **08:17 회차로 17/17 완주 예정** (마지막 2개: takopi-discord, sshc). 완주 후 task 비활성화 가능 (`schtasks /Change /TN seCall-WikiDeepLoop /DISABLE`).

## 5. 다음 우선순위

1. **AA 라이브 반영** — 설치본이 #124 이전이라 재리빌드 → recall `related_sessions` 에 AA 적용.
2. **Graph insights** (graph review 결론, `docs/reference/core-backlog` 참고): **surprising connections**(AA 위 얇은 필터: 관련도 높은데 직접 엣지 없는 쌍) + **knowledge gaps**(degree 집계: 고립 세션/싱글턴 file·issue, `graph stats`/`run_stats` 에 추가). AA 기반 저비용 후속.
3. **Louvain community detection** — 세션유사도 투영(AA) 선행 필요, 증분빌드 전역재계산 → 큰 별건.
4. 기존 백로그: Task C model discovery(Rust, 캡 무관) · Phase B(Kiwi Windows + raw-token FTS, debt) · 임베딩 model_id 무효화 잠복버그 · 위키 vault 확정 커밋 · 1293 복구불가(parked).

## 6. 핵심 교훈

- **CI green 확인 후 머지** — `--auto` 즉시머지로 #121~#123 이 red main 을 방치, 심지어 **v0.6.5 를 red main 에서 릴리스**함(기능 정상·테스트 회귀였으나 프로세스 실패). 머지 전 `gh pr checks <N>`; **integration 테스트(`tests/*.rs`)는 CI 에서만** 도니 로컬 `--lib` 만으론 부족. (메모리 `feedback_ci_before_merge`)
- 로컬 전체 `cargo test` 페이징부족(os error 1455) → 단일 바이너리(`--test <name>`)로 분할 검증.
- Fable 5 = 최고가 플래그십(Opus 초과) — 벌크 위키는 Opus.
