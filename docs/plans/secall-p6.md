# seCall P6 — 안정성 + 성능 개선

> Phase: approval | Status: draft | Revision: 0
> Generated by tunaFlow

---

## Description

P5 GitHub 연동 테스트에서 발견된 결함 2건(ANN capacity 경고, sync pull 실패)과 ingest 성능 병목을 해결.
실사용 안정성 확보가 목적이며, 기능 추가 없이 기존 기능의 품질을 올리는 데 집중.

## Expected Outcome

- `secall sync` 실행 시 ANN 경고 0건
- 이전 sync 실패 후에도 다음 sync가 정상 동작 (pull 전 자동 commit/stash)
- ingest 임베딩 속도 개선 (세션별 순차 → 배치 병렬화)
- `secall sync` 1회 실행이 2분 이하로 완료 (현재 ~5분)

## Subtasks

### 1. ANN reserve 사전 할당

- **Status**: todo

#### 작업 지시

`ann.rs:25-29`에서 loaded index에도 `reserve()` 호출. 신규 insert 전 capacity 체크 추가. 공수: Small

### 2. Sync pull 안전성

- **Status**: todo

#### 작업 지시

`sync.rs:22-41` Phase 1 전에 unstaged 변경 감지 → 자동 commit 또는 stash. `git.rs`에 `auto_commit()` 메서드 추가. 공수: Small

### 3. Ingest 임베딩 병렬화

- **Status**: todo

#### 작업 지시

`vector.rs:47-104`의 세션별 순차 임베딩을 `tokio::JoinSet` 기반 병렬 처리로 전환. OrtEmbedder의 `Arc<Mutex<Session>>` 활용. 공수: Medium

## Revision History

- `2026-04-06 10:03` promoted (user) — Promoted from chat
- `2026-04-06 10:03` detail design requested (system) — Auto-requested on promotion
- `2026-04-06 10:03` subtask review started (user)
- `2026-04-06 10:03` subtask review completed (user)

